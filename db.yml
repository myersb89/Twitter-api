# Use postgres/example user/password credentials
version: '3.1'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: twitter_data
      POSTGRES_PASSWORD: test1234
    ports:
      - 5432:5432
